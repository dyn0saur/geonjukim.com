# Plan

## Phases

### Phase 0 — 요구사항 및 기술 검토

핵심 경험을 상태 기반 노드 캔버스로 구현할 수 있는지 검토하고, 확정 규칙과 미결정을 분리한다.

### Phase 1 — 핵심 인터랙션 프로토타입

`5 + 3 → 8` 고정 시나리오로 이동, Bézier 연결, 경고, 줌, 스냅의 성립 여부를 검증한다.

### Phase 2 — 포트폴리오 시나리오 시스템

프로젝트별 노드 구성과 액션-결과 전이를 데이터로 선언할 수 있는 구조를 만든다.

#### Phase 2-1 — HANA HQ 프로젝트 캔버스 MVP

HANA HQ의 Ref. Surface 해체, Wood Panel 제작 요구사항, 3D 구성, 2D 도면 생성 흐름을 첫 데이터 기반 프로젝트 시나리오로 구현한다.

### Phase 3 — 3D 결과 노드

Three.js 기반 3D 뷰어를 노드에 통합하고 Rhino 모델 전달 형식과 입력 이벤트 정책을 확정한다.

### Phase 4 — 콘텐츠 및 제품화

실제 프로젝트 콘텐츠, 반응형/접근성, 성능, 내비게이션, 배포 품질을 완성한다.

### Phase 5 — 호스팅 독립화 및 이전

Sites에서 완성한 애플리케이션의 플랫폼 의존성을 감사하고 Cloudflare 등 선정된 독립 호스팅으로 이전한다.

## Plans

### Phase 0 tasks

- [x] 목표 경험과 비계산형 고정 시나리오를 정의한다.
- [x] 요구된 색상과 기본 시각 규칙을 기록한다.
- [x] 구현 가능성, 권장 상태 모델, 주요 위험을 정리한다.
- [x] 격자 15×5단위와 이동 중 정렬 스냅 규칙을 확정한다.
- [x] 300%에서 1단위=1 CSS px, 20%~300% 줌, 우클릭 pan을 확정한다.

### Phase 1 tasks

- [x] 하나의 월드 좌표계와 viewport pan/zoom 변환을 구현한다.
- [x] 배경색과 15×5단위 격자를 viewport에 맞게 렌더링한다.
- [x] 패널, 덧셈 컴포넌트, 포트의 정상/경고 시각 상태를 구현한다.
- [x] 노드 드래그와 외곽·중앙·포트 축 정렬 스냅을 구현한다.
- [x] 출력 포트에서 입력 포트까지 임시 cubic Bézier 와이어를 구현한다.
- [x] 유효 포트 판정, 연결 확정, 취소를 구현한다.
- [x] 누락된 A 입력의 경고 표시와 hover/focus 설명 메시지를 구현한다.
- [x] `5 → A` 연결 시 경고 해제 및 결과 `8` 전이를 구현한다.
- [x] 패널 텍스트의 중앙 정렬, 연결 드래그 화살표 및 Ctrl+드래그 연결 삭제를 구현한다.
- [x] 배경 그리드 시각 셀 간격을 40배 확대한다.
- [x] 공통 월드 변환으로 노드 이동·pan·zoom 시 포트와 와이어의 좌표 일치를 구현한다.
- [ ] 서로 다른 device pixel ratio와 브라우저 크기에서 1월드 단위의 렌더링을 검증한다.
- [x] 프로토타입 구현 결과와 후속 브라우저 QA 필요성을 기록한다.

### Phase 2 tasks

- [x] 노드, 포트, 연결, 시나리오 전이의 데이터 스키마를 정의한다.
- [x] 프로젝트별 초기 상태와 허용 액션을 데이터로 로드한다.
- [ ] 프로젝트 탐색, 상태 초기화, 완료 상태의 UX를 정의한다.
- [ ] 큰 캔버스에서 보이지 않는 콘텐츠의 렌더링/탐색 성능을 검증한다.

### Phase 2-1 tasks

- [x] 상충하는 초기 연결 상태, 2D 결과 뷰어 타입, 표시 철자, 아이콘 범위와 초기 viewport를 확정한다.
- [x] 제목, 노드, 포트, 패널, 뷰어와 연결을 선언하는 HANA HQ 시나리오 데이터를 정의한다.
- [x] Phase 1의 하드코딩된 노드·와이어 렌더링을 데이터 기반 렌더러로 확장한다.
- [x] `HANA HQ` 제목 Scribble과 전체 흐름을 좌에서 우로 배치한다.
- [x] Ref. SRF 뷰어·연결 컴포넌트와 Deconstruct 입력/출력 구성을 구현한다.
- [x] Wood Panel의 fabrication 요구사항 패널과 Construct 입력 연결 구성을 구현한다.
- [x] 빈 3D 모델 뷰어와 2D 이미지 뷰어 placeholder를 구현한다.
- [x] 주요 컴포넌트용 Grasshopper 스타일 PNG 아이콘을 생성·검수하고 프로젝트 자산으로 연결한다.
- [x] Bifocals 스타일 이름 라벨, 단일 선택 색상과 선택 와이어 그라데이션을 구현한다.
- [x] 사용자가 완성해야 하는 연결과 그에 따른 뷰어 상태 전이를 구현한다.
- [x] 초기 상태, 허용/거부 연결, 선택, 이동, pan/zoom과 서버 렌더 결과를 테스트한다.
- [x] 브라우저 렌더를 시각 검수하고 검증된 비공개 Sites 버전을 게시한다.
- [x] 연결 블록·단색 패널·3열 주요 컴포넌트·공통 서체·우상단 경고 말풍선으로 HANA HQ 시각 체계를 재정리한다.
- [x] 네 처리 아이콘을 단순 도형과 제한 단색 팔레트의 Grasshopper 도구 아이콘 문법으로 다시 제작한다.
- [x] 모델·이미지 뷰어 기본 크기를 2.5배로 키우고 네 모서리 자유 리사이즈 및 동적 와이어 좌표를 구현한다.
- [x] 좌클릭 Fence 선택, 다중 선택 집합의 상대 위치 유지 그룹 이동, 선택 집합 와이어 상태를 구현한다.
- [x] 포트를 컴포넌트 뒤에 절반이 가려진 반원으로 변경하고 연결 추가·Ctrl 해제를 브라우저에서 검증한다.
- [x] 우측 하단 상태 표시에서 연결 단계 정보를 제거한다.

### Phase 3 tasks

- [ ] Rhino 모델의 웹 전달 형식과 변환 파이프라인을 결정한다.
- [ ] Three.js 뷰어 노드의 로딩·오류·완료 상태를 구현한다.
- [ ] 3D orbit/zoom과 노드 캔버스 pan/zoom의 입력 우선순위를 구현한다.
- [ ] GPU 및 메모리 예산을 정하고 다수 뷰어가 있는 장면을 최적화한다.

### Phase 4 tasks

- [ ] 실제 포트폴리오 프로젝트를 시나리오와 3D 자산으로 작성한다.
- [ ] 키보드, 터치, reduced motion, 화면 크기별 사용성을 완성한다.
- [ ] 초기 로딩, 자산 스트리밍, 오류 복구, 브라우저 호환성을 검증한다.
- [ ] Sites 환경에서 초기 배포 및 분석 체계를 구성한다.
- [x] 검증을 통과한 구현 수정마다 현재 비공개 Sites 버전을 갱신하는 운영 규칙을 확정한다.
- [ ] 호스팅 이전에 필요한 트래픽, 3D 자산 용량, 캐시, 비용, 도메인 및 분석 요구를 측정한다.

### Phase 5 tasks

- [ ] 코드와 설정에서 ChatGPT Sites 전용 의존성을 목록화한다.
- [ ] Cloudflare를 포함한 독립 호스팅 후보를 비용, 런타임, 정적·3D 자산 전달, 캐시, 분석 및 운영성 기준으로 비교한다.
- [ ] 최종 호스팅 환경과 배포 방식을 결정한다.
- [ ] 환경 변수, 저장소 및 제공자 전용 기능을 어댑터 경계로 리팩터링한다.
- [ ] 선택한 호스팅 환경의 빌드 및 배포 구성을 추가한다.
- [ ] 정적 자산과 3D 모델을 새 스토리지·CDN 구조로 이전하고 URL 하드코딩이 없는지 검증한다.
- [ ] 핵심 인터랙션, 접근성, 성능 및 오류 복구 테스트를 새 환경에서 재실행한다.
- [ ] `geonjukim.com` DNS와 SSL을 새 환경에 연결하고 전환 전후 동작을 검증한다.
- [ ] 안정화 기간 후 기존 Sites 배포의 유지·비공개 전환·종료 여부를 결정한다.
- [ ] 롤백 절차와 최종 운영 문서를 작성한다.
